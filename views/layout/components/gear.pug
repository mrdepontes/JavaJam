#MainContent.box
  .container
    // Saved Space for other Opjects
    #promotion.slogan
      span
        | Get a 10% discount when you wear a JavaJam shirt or bring
        center
        |  in your JavaJam mug!
      h1
        | JAVAJAM GEAR
      p
        | JavaJam gear not only looks good, it&apos;s good to your wallet, too.
    form(method='post' action='http://www.webdevfoundations.net/scripts/cart.asp')
      each tour, index in tours    
        .item
          p=tour.tourBlurb
          img.clearright(src=`images/${tour.tourImage}`)
          input#desc2(type='hidden' name=`des${index}` value=`${tour.tourRegion}`)
          input#cost2(type='hidden' name=`cost${index}` value=`${tour.tourPrice}`)
          button(type='submit') Add to Cart
        